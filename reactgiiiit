 {open && (
        <div ref={calendarRef}>
          <Calendar
            date={new Date()}
            onChange={handleSelect}
            className="calendarElement"
          />
        </div>
      )}

<input
        value={calendar}
        readOnly
        className="inputBox"
        onClick={() => setOpen((open) => !open)}
      />

